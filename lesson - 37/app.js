// functtionni tahlil qilish
// Tahlil

function calcAge(birthYear,firsName) {
	const age = 2026 - birthYear
	console.log(`${firsName}ning yoshi ${age}`);
	return age
}
const newAge = calcAge(2000,'Shokirjon')